<?xml version="1.0" encoding="utf8"?>
<shader_source>
 <name>esm</name>
 <type>shadow_mapping</type>
 <shader_source_datas>
  <shader_source_data>
   <parameters>
    <parameter>
     <common_parameter>shadow</common_parameter>
     <qualifier>out</qualifier>
    </parameter>
    <parameter>
     <auto_parameter>shadow_camera</auto_parameter>
    </parameter>
    <parameter>
     <sampler_parameter>shadow_sampler</sampler_parameter>
     <type>sampler2DArray</type>
    </parameter>
    <parameter>
     <common_parameter>shadow_uvs</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
    <parameter>
     <common_parameter>world_position</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
   </parameters>
   <source>vec3 uv;
vec2 lod_uv, ddx, ddy;
float occluder, light_distance;
int i;

const float light_shadow_bias = -0.01;
const float over_darkening_factor = 2.5;

shadow = 1.0;

lod_uv = shadow_uvs[shadow_map_count - 1].xy / shadow_uvs[shadow_map_count - 1].w;

ddx = dFdx(lod_uv);
ddy = dFdy(lod_uv);

for (i = 0; i &lt; shadow_map_count; ++i)
{
	uv = shadow_uvs[i].xyz / shadow_uvs[i].w;

	if (all(lessThan(abs(uv - vec3(0.5)), vec3(0.4995))))
	{
		occluder = textureGrad(shadow_sampler, vec3(uv.xy, i), ddx, ddy).r;
		occluder += textureGradOffset(shadow_sampler, vec3(uv.xy, i), ddx, ddy, ivec2(-1, -1)).r;
		occluder += textureGradOffset(shadow_sampler, vec3(uv.xy, i), ddx, ddy, ivec2(-1,  1)).r;
		occluder += textureGradOffset(shadow_sampler, vec3(uv.xy, i), ddx, ddy, ivec2( 1, -1)).r;
		occluder += textureGradOffset(shadow_sampler, vec3(uv.xy, i), ddx, ddy, ivec2( 1,  1)).r;
		occluder *= 0.2;

		light_distance = distance(shadow_camera, world_position) + light_shadow_bias;

		shadow = exp(over_darkening_factor * (log(occluder) - light_distance));
		shadow = clamp(shadow, 0.0, 1.0);
		break;
	}
}
</source>
   <version>130</version>
  </shader_source_data>
 </shader_source_datas>
</shader_source>
