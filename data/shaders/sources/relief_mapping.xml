<?xml version="1.0" encoding="utf8"?>
<shader_source>
 <name>relief_mapping</name>
 <type>normal_mapping</type>
 <shader_source_datas>
  <shader_source_data>
   <parameters>
    <parameter>
     <common_parameter>fragment_normal</common_parameter>
     <qualifier>out</qualifier>
    </parameter>
    <parameter>
     <common_parameter>fragment_uv</common_parameter>
     <qualifier>out</qualifier>
    </parameter>
    <parameter>
     <sampler_parameter>normal_sampler</sampler_parameter>
     <type>sampler2D</type>
    </parameter>
    <parameter>
     <common_parameter>tbn_matrix</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
    <parameter>
     <common_parameter>world_uv</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
    <parameter>
     <common_parameter>world_view_direction</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
   </parameters>
   <source>{
	vec4 normal;
	float h;
	int i;

	float depth_factor = 0.05;

	vec3 V = world_view_direction.xyz;
	float a = dot(tbn_matrix[2], -V);
	vec2 s = vec2(dot(V, tbn_matrix[0]), dot(V, tbn_matrix[1]));
	s *= depth_factor / a;
	vec2 ds = s;
	vec2 dp = world_uv;

   const int linear_search_steps = 15;

   float size = 1.0 / float(linear_search_steps);
   float depth = 0.0;
   float best_depth = 1.0;

   for(int i = 0; i &lt; linear_search_steps - 1; ++i)
   {
      depth += size;
      float t = 1.0 - texture2D(normal_sampler, dp + ds * depth).a;
      if(best_depth &gt; 0.996)
         if(depth &gt;= t)
            best_depth = depth;
   }
   depth = best_depth;

   const int binary_search_steps = 6;

   for(int i = 0; i &lt; binary_search_steps; ++i)
   {
      size *= 0.5;
      float t = 1.0 - texture2D(normal_sampler, dp + ds * depth).a;
      if(depth &gt;= t)
      {
         best_depth = depth;
         depth -= 2.0 * size;
      }
      depth += size;
   }

	float d = best_depth;

	vec2 uv = dp + ds * d;

	normal = texture2D(normal_sampler, uv.xy);
	normal.z = 1.0 - sqrt(dot(normal.xy, normal.xy));
	normal.xyz = normalize(tbn_matrix * normal.xyz);
	fragment_normal = normal.xyz;
	fragment_uv = uv.xy;
}
</source>
   <version>120</version>
  </shader_source_data>
  <shader_source_data>
   <parameters>
    <parameter>
     <common_parameter>fragment_normal</common_parameter>
     <qualifier>out</qualifier>
    </parameter>
    <parameter>
     <common_parameter>fragment_uv</common_parameter>
     <qualifier>out</qualifier>
    </parameter>
    <parameter>
     <sampler_parameter>normal_sampler</sampler_parameter>
     <type>sampler2D</type>
    </parameter>
    <parameter>
     <common_parameter>tbn_matrix</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
    <parameter>
     <common_parameter>world_uv</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
    <parameter>
     <common_parameter>world_view_direction</common_parameter>
     <qualifier>in</qualifier>
    </parameter>
   </parameters>
   <source>{
	vec4 normal;
	float h;
	int i;

	float depth_factor = 0.05;

	vec3 V = world_view_direction.xyz;
	float a = dot(tbn_matrix[2], -V);
	vec2 s = vec2(dot(V, tbn_matrix[0]), dot(V, tbn_matrix[1]));
	s *= depth_factor / a;
	vec2 ds = s;
	vec2 dp = world_uv;

   const int linear_search_steps = 15;

   float size = 1.0 / float(linear_search_steps);
   float depth = 0.0;
   float best_depth = 1.0;

   for(int i = 0; i &lt; linear_search_steps - 1; ++i)
   {
      depth += size;
      float t = 1.0 - texture(normal_sampler, dp + ds * depth).a;
      if(best_depth &gt; 0.996)
         if(depth &gt;= t)
            best_depth = depth;
   }
   depth = best_depth;

   const int binary_search_steps = 6;

   for(int i = 0; i &lt; binary_search_steps; ++i)
   {
      size *= 0.5;
      float t = 1.0 - texture(normal_sampler, dp + ds * depth).a;
      if(depth &gt;= t)
      {
         best_depth = depth;
         depth -= 2.0 * size;
      }
      depth += size;
   }

	float d = best_depth;

	vec2 uv = dp + ds * d;

	normal = texture(normal_sampler, uv.xy);
	normal.z = 1.0 - sqrt(dot(normal.xy, normal.xy));
	normal.xyz = normalize(tbn_matrix * normal.xyz);
	fragment_normal = normal.xyz;
	fragment_uv = uv.xy;
}
</source>
   <version>120</version>
  </shader_source_data>
 </shader_source_datas>
</shader_source>
